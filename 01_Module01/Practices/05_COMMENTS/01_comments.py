# useful to leave additional information to code
# comments are omitted at runtime
# variable names need to give as self-commenting itself; like width, length, height instead of using var1, 2, 3
# to comment/uncomment multiple lines at a time, use ctrl + /
